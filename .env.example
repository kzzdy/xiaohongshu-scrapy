# 小红书爬虫配置文件模板
# 复制此文件为 .env 并填入实际配置

# ============================================
# 必填配置
# ============================================

# 小红书Cookie（必填）
# 获取方式：登录小红书网页版，打开浏览器开发者工具，复制Cookie
COOKIES='your_cookies_here'

# ============================================
# 可选配置
# ============================================

# 速率限制（每秒请求数，默认3.0）
RATE_LIMIT=3.0

# 请求超时时间（秒，默认30）
TIMEOUT=30

# 重试次数（默认3）
RETRY_TIMES=3

# 代理配置（可选，格式：http://host:port 或 https://host:port）
# HTTP_PROXY=
# HTTPS_PROXY=

# 日志级别（可选，可选值：DEBUG, INFO, WARNING, ERROR，默认INFO）
LOG_LEVEL=INFO

# 数据输出目录（默认datas）
OUTPUT_DIR=datas

# 进度文件路径（默认datas/.progress.json）
PROGRESS_FILE=datas/.progress.json

# 是否启用断点续传（默认true）
ENABLE_RESUME=true

# 下载媒体文件（图片/视频）（默认true）
DOWNLOAD_MEDIA=true

# 最大并发下载数（默认3）
MAX_CONCURRENT_DOWNLOADS=3
